# 🍊 浩泽的橘子窝 · 使用说明

这个博客用 **Astro** 搭建。Astro 是个命令行工具，它的提示信息**默认是英文**（官方没有中文版）。
为了让您不看英文也能顺利使用，本文把日常操作全部翻译成中文，并提供了双击即用的中文脚本。

---

## ⭐ 首选：用「编辑后台」写文章（网页编辑器）

不用再手动改文件了！双击 **启动编辑后台.cmd**，浏览器会自动打开编辑后台（http://localhost:4322），顶部有「文章 / 站点设置 / 桌宠 / 网站收集」几个页面：

- **写文章**（📝 文章）：左边写、右边实时预览，标题/简介/日期/分类/标签都是填表单
- **插图排版**：正文里点工具栏的图片按钮插入图片后，会弹出小面板——可以拖宽度、选左/中/右对齐、删除图片；直接粘贴图片网址也会自动变成图片
- **新建目录（分类）**：把文章分门别类，网站会自动生成分类页
- **站点设置**（⚙️）：改站点名、首页大标题/副标题、页脚文字、顶部导航、关于页内容
- **桌宠**（🐱）与 **网站收集**（🔖）：见下面两节
- **一键发布**：写完后点「发布到网上」，自动推送到 GitHub，网站自动更新

> 编辑后台只是帮你改文件——文章仍然是普通 Markdown，存在 `src/content/blog/` 里，随时可以手动改。

---

## ⭐⭐ 桌宠与 AI 聊天（右下角的小橘子）

博客右下角有一只小桌宠（前端页面）：

- **鼠标靠近**：冒出气泡问候，几秒后自动消失
- **单击桌宠**：打开聊天框，可以和它对话（AI 回答）
- **拖拽**：可以把它拖到屏幕任意位置

**后台 → 🐱 桌宠** 里可以配置：

- **AI 配置**：接口地址（OpenAI 兼容）、API Key、模型名、温度、最大回复字数
- **人格设置**：角色名、系统提示词（决定它怎么说话）
- **气泡**：开关、文案（一行一条）、触发距离、显示时长
- **工具**：让 AI 能"搜索博客文章"、"看当前时间"
- **MCP 接入**：预留项，需服务端环境，暂未启用

> ⚠️ 注意：AI 的 API Key 会写进网页源码并随仓库公开，**请用一个额度受限、可随时吊销的 Key**，别用主力账号的 Key。
> 没填 Key 时，点桌宠聊天会提示"还没配置好"。

## ⭐⭐ 站点设置：改全站文案

1. 顶部点 **⚙️ 站点设置**
2. 可修改：站点名称、站点简介、首页大标题、首页副标题、页脚文字、顶部导航链接（「首页」固定在最前）、关于页内容
3. 点「保存设置」，再点顶部「发布到网上」，改完的文案就更新到网站

> 关于页内容支持 HTML；需要站内链接时用 `{%base%}` 代替站点前缀，例如 `{%base%}blog/我的文章`。

---

## 一、常用操作（双击脚本即可）

| 操作 | 双击哪个文件 | 效果 |
|------|-------------|------|
| 网页写文章 | **启动编辑后台.cmd** | 打开编辑后台 http://localhost:4322，自动拉起网站 |
| 写文章/预览 | **启动博客.cmd** | 打开开发服务器，浏览器访问 http://localhost:4321/haoze-orange/ |
| 关掉服务器 | **停止服务器.cmd** | 停止刚才的开发服务器 |
| 生成上线文件 | **构建站点.cmd** | 把整站生成到 `dist/` 文件夹 |
| 预览构建结果 | **本地预览.cmd** | 预览 `dist/` 里构建好的网站 |

> 电脑提示「无法运行 .cmd」时：右键文件 → 用终端运行，或在文件夹地址栏输入 `cmd` 再执行。

---

## 二、用命令行操作（想学的时候看这里）

在项目文件夹 `D:\AI\博客` 下运行：

| 作用 | 英文命令 | 中文意思 |
|------|---------|---------|
| 启动 | `npm run dev` | 打开开发服务器 |
| 停止 | `npx astro dev stop` | 停止开发服务器 |
| 查看状态 | `npx astro dev status` | 看服务器有没有在运行 |
| 看日志 | `npx astro dev logs` | 查看服务器日志 |
| 构建 | `npm run build` | 生成上线用的静态文件 |
| 预览 | `npm run preview` | 本地预览构建结果 |

---

## 三、Astro 提示英文是什么意思（对照表）

启动后终端常见英文提示：

| 英文提示 | 中文意思 |
|---------|---------|
| `Dev server running at http://127.0.0.1:4321` | ✅ 开发服务器已启动 |
| `Stop: astro dev stop` | 停止命令是 `astro dev stop` |
| `Status: astro dev status` | 查看状态的命令 |
| `Logs: astro dev logs` | 查看日志的命令 |
| `Local: http://localhost:4321/haoze-orange/` | 在浏览器打开这个地址 |
| `✓ Completed` | 构建/编译完成 |
| `Complete!` | 全部完成 |
| `error / failed` | 出错了（把这段文字发给我，我帮您看） |

---

## 四、写一篇新文章（核心技能）

1. 打开文件夹 `src/content/blog/`
2. 新建一个文件，比如 `我的第一篇.md`
3. 文件开头写一段「档案」（叫 frontmatter）：

```markdown
---
title: '文章标题'
description: '一句话简介'
date: 2026-08-01
tags: ['生活', '技术']
---
```

4. 下面写正文（支持 `**加粗**`、列表、代码块等 Markdown 语法）
5. 保存 → 刷新浏览器，首页、标签、搜索会自动更新

> 日期格式是 `年-月-日`，如 `2026-08-01`。

---

## 五、发布到网上（GitHub Pages）

推送后网站自动上线，无需手动操作：

1. 在 GitHub 新建仓库（如 `orange-nest`，先不要勾选初始化 README）
2. 修改 `astro.config.mjs` 里的 `site` 和 `base` 两个值
3. 推送代码：

```bash
git remote add origin https://github.com/<你的用户名>/<仓库名>.git
git push -u origin main
```

4. 在 GitHub 仓库 **Settings → Pages**，Source 选 **GitHub Actions**

以后每次 `git push`，网站自动更新。

---

## 六、改外观（配色/字体）

所有颜色都在 `src/styles/global.css` 开头的变量里，改一个值全站生效。
比如想把主色从橙色改成绿色，把 `--accent` 的值换成 `#22c55e` 即可。
