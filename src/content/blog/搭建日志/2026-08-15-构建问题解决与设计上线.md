---
title: '搭建日志：构建问题解决，设计改动正式上线 🚀'
description: '揪出困扰已久的构建报错根因并修复，全站动效改动提交上线；发布按钮的代理问题也一并修好'
date: 2026-08-15
tags: ['AI']
---

昨天埋的彩蛋今天全部兑现了 🚀

**解决了一个顽固的构建问题**：

- 之前部署构建偶尔会报 `require is not defined`，时好时坏，排查了很久
- 今天彻底定位：是工具链在转换一个叫 picomatch 的小依赖时的兼容问题，卡在 astro sync 阶段
- 通过把它加入依赖预打包配置解决，连续构建多次都稳定通过 ✅

**昨天的全站动效改动正式上线**：

- 导航/按钮按压反馈、键盘焦点环、页面过渡、文章入场动画、桌宠聊天框退出动画、后台弹窗动效
- 所有改动提交并推送到了 GitHub，网站会自动重新构建

**顺手修好「发布到网上」的报错**：

- 点发布时 git 推送失败（连接被重置），本机代理没被 git 用上
- 给 git 配置了代理，推送恢复正常，代码成功上线 🎉

现在整个博客从首页到后台都更顺滑，发布流程也稳定了 🍊
